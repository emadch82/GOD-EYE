[package]
name = "ai_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "ai_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
numpy = { version = "0.23" }
ndarray = "0.16"
image = { version = "0.25", features = ["jpeg", "png"] }
